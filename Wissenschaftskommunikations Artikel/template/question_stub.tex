\documentclass{article}

\usepackage{tikz}

\begin{document}

\BLOCK{ for question in dict_questions }
\BLOCK{ set question_name = question}
\begin{figure}
    \centering
    \begin{tikzpicture}
        \BLOCK{ for numeral in dict_questions[question] }
            {# if numeral == 0}
            \BLOCK{ set decl = 0 + loop.index0 }
            \draw[white, fill=white] ( \VAR{ decl } ,0) rectangle (\VAR{ decl },3);
            {# else}
            \BLOCK{ set decl = 0 + loop.index0 }
            \draw[black, fill=black] (\VAR{ decl } ,0) rectangle (\VAR{ decl },3);
            {# endif }
        \BLOCK{ endfor }
    \end{tikzpicture}
    \caption{Barcode für Frage: \VAR{ question_name } }
\end{figure}
\BLOCK{ endfor }

\end{document}