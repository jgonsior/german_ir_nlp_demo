\documentclass{slides}

\usepackage{graphicx}
\usepackage{pdflscape}
\usepackage{tikz}

\begin{document}
\begin{landscape}
\begin{figure}
\scalebox{0.1}{
    \begin{tikzpicture}
        \BLOCK{ for numeral in barcode }
        # if numeral == 0
        \BLOCK{ set decl = 0 + loop.index0 }
        \draw[white, fill=white] ( \VAR{ decl } ,0) rectangle (\VAR{ decl },10);
        # else
        \BLOCK{ set decl = 0 + loop.index0 }
        \draw[black, fill=black] (\VAR{ decl } ,0) rectangle (\VAR{ decl },10);
        # endif
        \BLOCK{ endfor }
    \end{tikzpicture}
    }
    \caption{Barcode für Dokument: \VAR{ barcode_name } }
\end{figure}
\end{landscape}
\end{document}